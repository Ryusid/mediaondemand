events {}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;
    
    # Docker DNS resolver
    resolver 127.0.0.11 valid=30s;

    upstream user_service {
        server user-service:3000;
    }
    upstream catalog_service {
        server catalog-service:3000;
    }
    upstream upload_service {
        server upload-service:3000;
    }
    upstream search_service {
        server search-service:3000;
    }
    upstream streaming_service {
        server streaming-service:3000;
    }

    server {
        listen 80;
        root /usr/share/nginx/html;
        index index.html;

        # Health check
        location /health {
            return 200 '{"status": "ok", "service": "mediaondemand-gateway"}';
            add_header Content-Type application/json;
        }

        # Note: CORS is handled by the backend microservices (Express cors() middleware)

        # Frontend Static Files
        location / {
            if ($request_method = 'OPTIONS') {
                return 204;
            }
            try_files $uri $uri/ /index.html;
            
            # Prevent caching of the index.html to ensure new deployments load immediately
            add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0";
            if_modified_since off;
            expires off;
            etag off;
        }

        # User Service
        location /api/users {
            if ($request_method = 'OPTIONS') {
                return 204;
            }
            proxy_pass http://user_service;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        # Catalog Service
        location /api/catalog {
            if ($request_method = 'OPTIONS') {
                return 204;
            }
            proxy_pass http://catalog_service;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        # Upload Service
        location /api/upload {
            if ($request_method = 'OPTIONS') {
                return 204;
            }
            proxy_pass http://upload_service;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            client_max_body_size 500M;
        }

        # Search Service
        location /api/search {
            if ($request_method = 'OPTIONS') {
                return 204;
            }
            proxy_pass http://search_service;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        # Streaming Service
        location /api/stream {
            if ($request_method = 'OPTIONS') {
                return 204;
            }
            proxy_pass http://streaming_service;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}
